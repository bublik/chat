- if controller_name != 'sessions'
  = link_to t('devise.titles.sign_in'), new_session_path(resource_name)
- if devise_mapping.recoverable? && controller_name != 'passwords'
  = link_to t('devise.titles.reset_password'), new_password_path(resource_name)


