%div
  %strong= t('.new_feedback')

- [:username, :email, :phone, :message, :remote_ip, :user_agent,:referer].each do |column|
  #{SiteFeedback.human_attribute_name(column)}: #{@feedback.read_attribute(column)}

%p{style: 'fornt-size: 0.9em; color: gray'}
  = t('notification.thanx_for_using')
